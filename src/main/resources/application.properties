spring.application.name=learningSecurity

# ===============================
# DATABASE (Docker + Local Safe)
# ===============================
#spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/covid_db
spring.datasource.url=jdbc:mysql://localhost:3306/covid_db
# spring.datasource.username=${DB_USERNAME}
spring.datasource.username=root
# spring.datasource.password=${DB_PASSWORD}
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===============================
# SERVER
# ===============================
server.port=8080

# ===============================
# JWT (EXTERNALIZED)
# ===============================
# jwt.secret=${JWT_SECRET}
jwt.secret=ThisIsASuperSecretKeyForJwt2025_ChangeMe

# ===============================
# MAIL (EXTERNALIZED)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# spring.mail.username=${MAIL_USERNAME}
spring.mail.username=pravesh111tripathi@gmail.com 
#spring.mail.password=${MAIL_PASSWORD}
spring.mail.password=pgojbemhnkaeojid
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
